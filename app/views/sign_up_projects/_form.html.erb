<%= form_for(@sign_up_project) do |f| %>
  <% if @sign_up_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sign_up_project.errors.count, "error") %> prohibited this sign_up_project from being saved:</h2>

      <ul>
      <% @sign_up_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
